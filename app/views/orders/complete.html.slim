.justify-center.py-8.px-4.bg-linear-to-t.from-info/60.to-base-100.bg-no-repeat.bg-center.bg-cover style="min-height: calc(100vh - 64px);"
  .container.max-w-3xl
    .text-center.mb-8
      / Heading
      h1.text-2xl.md:text-4xl.font-bold.text-primary.mb-4 ขอบคุณสำหรับการสมัครสมาชิก
      p.text-lg.lg:text-xl.text-base-content.opacity-70 คุณสามารถเริ่มใช้งานแพ็กเกจสมาชิกได้แล้ว

    / Order Summary Card
    .card.bg-base-100.shadow-xl.p-9.rounded-3xl
      .flex.flex-col
        .text-center
          / Success Icon
          .inline-flex.items-center.justify-center.rounded-full
            span.iconify.lucide--check-circle.text-primary style="font-size: 4rem;"

        h2.card-title.text-xl.lg:text-3xl.mb-2.justify-center.text-center ทำรายการเสร็จสมบูรณ์


        .my-4.border-primary.border-t-1

        / Order Details
        .space-y-4
          .flex.flex-col.md:flex-row.justify-between.md:items-center
            span.text-lg.lg:text-2xl.opacity-70 หมายเลขคำสั่งซื้อ
            span.text-lg.lg:text-2xl = order_number(@order)

          .flex.flex-col.md:flex-row.justify-between.md:items-center
            span.text-lg.lg:text-2xl.opacity-70 ยอดชำระรวม
            span.text-lg.lg:text-2xl = number_to_currency(@order.total, unit: "฿", precision: 0)

          .flex.flex-col.md:flex-row.justify-between.md:items-center
            span.text-lg.lg:text-2xl.opacity-70 สถานะ
            div class="flex text-[#008638] text-lg lg:text-2xl gap-1 items-center"
              = image_tag "icons/check-fill-green.svg", alt: "check", class: "size-4 lg:size-6"
              span ชำระเงินแล้ว

    / Actions
    .flex.flex-col.lg:flex-row.justify-center.items-center.mt-4.gap-4.max-w-3xl.w-full
      - if @order.receipt_number.present?
        = link_to receipt_order_path(@order), class: "btn bg-white border-1 border-primary rounded-full btn-lg flex-1 text-primary max-lg:w-full max-lg:py-2" do
          span ดาวน์โหลดใบเสร็จ

      = link_to library_path, class: "btn btn-primary btn-lg flex-1 rounded-full max-lg:w-full max-lg:py-2" do
        span ไปที่คลังหนังสือพิมพ์


        / = link_to "#", class: "btn btn-ghost btn-sm gap-2" do
        /   span.iconify.lucide--help-circle
        /   span Need Help?
